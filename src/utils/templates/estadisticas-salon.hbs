<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Estadísticas por Salón</title>
    <style>
        body { 
            font-family: 'Arial', sans-serif; 
            margin: 20px; 
            color: #333;
            font-size: 12px;
        }
        .header { 
            text-align: center; 
            margin-bottom: 20px;
            border-bottom: 2px solid #333;
            padding-bottom: 10px;
        }
        .header h1 { 
            margin: 0; 
            color: #2c3e50;
            font-size: 18px;
        }
        .resumen {
            background: #f8f9fa;
            padding: 10px;
            border-radius: 5px;
            margin-bottom: 15px;
            text-align: center;
        }
        .tabla { 
            width: 100%; 
            border-collapse: collapse; 
            margin-top: 10px;
            font-size: 11px;
        }
        .tabla th, .tabla td { 
            border: 1px solid #ddd; 
            padding: 6px; 
            text-align: left; 
        }
        .tabla th { 
            background-color: #2c3e50; 
            color: white; 
        }
        .tabla tr:nth-child(even) {
            background-color: #f9f9f9;
        }
        .fecha {
            font-size: 10px;
            color: #7f8c8d;
            text-align: right;
        }
        .total-row {
            font-weight: bold;
            background-color: #e9ecef !important;
        }
        .detalle-reserva {
            font-size: 9px;
            color: #666;
            background-color: #f8f9fa;
        }
    </style>
</head>
<body>
    <div class="header">
        <h1>Estadísticas por Salón</h1>
        <div class="fecha">Generado el: {{fechaGeneracion}}</div>
    </div>

    <div class="resumen">
        <strong>Total Salones:</strong> {{total_salones}} | 
        <strong>Total Reservas:</strong> {{total_reservas}} | 
        <strong>Ingresos Totales:</strong> ${{ingresos_totales}}
    </div>

    {{#if datos.length}}
    <table class="tabla">
        <thead>
            <tr>
                <th>Salón</th>
                <th>Total Reservas</th>
                <th>Ingresos</th>
                <th>Promedio por Reserva</th>
            </tr>
        </thead>
        <tbody>
            {{#each datos}}
                <tr>
                    <td><strong>{{this.salon}}</strong></td>
                    <td>{{this.total_reservas}}</td>
                    <td>${{this.ingresos_salon}}</td>
                    <td>${{this.promedio_por_reserva}}</td>
                </tr>
                
                {{! Si necesitas mostrar detalles de reservas, necesitarías pasar esos datos }}
                {{! Por ahora comentamos esta sección hasta que tengas los datos }}
                {{!--
                {{#if this.reservas_detalle}}
                    {{#each this.reservas_detalle}}
                    <tr class="detalle-reserva">
                        <td colspan="4" style="padding-left: 20px;">
                            📅 Reserva #{{reserva_id}} | 👤 {{cliente}} | ⏰ {{hora_desde}}-{{hora_hasta}} | 💰 ${{importe_total}}
                        </td>
                    </tr>
                    {{/each}}
                {{/if}}
                --}}
            {{/each}}
        </tbody>
    </table>
    {{else}}
    <div style="text-align: center; color: #666; font-style: italic; padding: 20px;">
        No hay datos de estadísticas por salón para mostrar
    </div>
    {{/if}}
</body>
</html>