<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Estadísticas Mensuales</title>
    <style>
        body { 
            font-family: 'Arial', sans-serif; 
            margin: 20px; 
            color: #333;
            font-size: 12px;
        }
        .header { 
            text-align: center; 
            margin-bottom: 20px;
            border-bottom: 2px solid #333;
            padding-bottom: 10px;
        }
        .header h1 { 
            margin: 0; 
            color: #2c3e50;
            font-size: 18px;
        }
        .resumen {
            background: #f8f9fa;
            padding: 10px;
            border-radius: 5px;
            margin-bottom: 15px;
            text-align: center;
        }
        .tabla { 
            width: 100%; 
            border-collapse: collapse; 
            margin-top: 10px;
            font-size: 11px;
        }
        .tabla th, .tabla td { 
            border: 1px solid #ddd; 
            padding: 6px; 
            text-align: left; 
        }
        .tabla th { 
            background-color: #2c3e50; 
            color: white; 
        }
        .tabla tr:nth-child(even) {
            background-color: #f9f9f9;
        }
        .fecha {
            font-size: 10px;
            color: #7f8c8d;
            text-align: right;
        }
        .total-row {
            font-weight: bold;
            background-color: #e9ecef !important;
        }
    </style>
</head>
<body>
    <div class="header">
        <h1>Estadísticas Mensuales - Año {{anio}}</h1>
        <div class="fecha">Generado el: {{fechaGeneracion}}</div>
    </div>

    <div class="resumen">
        <strong>Total Reservas:</strong> {{total_reservas}} | 
        <strong>Ingresos Totales:</strong> ${{ingresos_totales}}
    </div>

    <table class="tabla">
        <thead>
            <tr>
                <th>Mes</th>
                <th>Total Reservas</th>
                <th>Ingresos Totales</th>
                <th>Promedio por Reserva</th>
            </tr>
        </thead>
        <tbody>
            {{#each datos}}
            <tr>
                <td>{{mes}}</td>
                <td>{{total_reservas}}</td>
                <td>${{ingresos_totales}}</td>
                <td>${{promedio}}</td>
            </tr>
            {{/each}}
            <tr class="total-row">
                <td><strong>TOTAL</strong></td>
                <td><strong>{{total_reservas}}</strong></td>
                <td><strong>${{ingresos_totales}}</strong></td>
                <td><strong>${{promedio_general}}</strong></td>
            </tr>
        </tbody>
    </table>
</body>
</html>