<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{titulo}}</title>
    <style>
        body { 
            font-family: 'Arial', sans-serif; 
            margin: 0; 
            padding: 20px; 
            color: #333;
            font-size: 12px;
        }
        .header { 
            text-align: center; 
            border-bottom: 2px solid #333; 
            padding-bottom: 10px;
            margin-bottom: 20px;
        }
        .header h1 { 
            margin: 0; 
            color: #2c3e50;
            font-size: 20px;
        }
        .info { 
            display: flex; 
            justify-content: space-between; 
            margin-bottom: 20px;
            font-size: 12px;
            background: #f8f9fa;
            padding: 10px;
            border-radius: 5px;
        }
        .reserva { 
            margin-bottom: 10px; 
            padding: 8px; 
            border: 1px solid #ddd; 
            border-radius: 3px;
            background-color: #f9f9f9;
            page-break-inside: avoid;
        }
        .reserva-header { 
            font-weight: bold; 
            color: #2c3e50; 
            margin-bottom: 4px;
            font-size: 11px;
        }
        .reserva-detalle { 
            display: grid; 
            grid-template-columns: 1fr 1fr; 
            gap: 5px; 
            font-size: 10px;
        }
        .totales {
            margin-top: 20px;
            padding: 10px;
            background-color: #2c3e50;
            color: white;
            border-radius: 5px;
            text-align: center;
            font-size: 12px;
        }
        .fecha {
            font-size: 10px;
            color: #7f8c8d;
            text-align: right;
        }
        .no-data {
            text-align: center;
            color: #7f8c8d;
            font-style: italic;
            padding: 20px;
        }
    </style>
</head>
<body>
    <div class="header">
        <h1>{{titulo}}</h1>
        <div class="fecha">
            Generado el: {{fechaGeneracion}}<br>
            {{periodo}}
        </div>
    </div>

    <div class="info">
        <div><strong>Total de Reservas:</strong> {{totalReservas}}</div>
        <div><strong>Ingresos Totales:</strong> ${{totalIngresos}}</div>
    </div>

    {{#if reservas.length}}
        {{#each reservas}}
        <div class="reserva">
            <div class="reserva-header">
                Reserva #{{reserva_id}} - {{fecha_reserva}}
            </div>
            <div class="reserva-detalle">
                <div><strong>Cliente:</strong> {{cliente}}</div>
                <div><strong>Teléfono:</strong> {{celular}}</div>
                <div><strong>Salón:</strong> {{salon}}</div>
                <div><strong>Turno:</strong> {{hora_desde}} - {{hora_hasta}}</div>
                <div><strong>Temática:</strong> {{tematica}}</div>
                <div><strong>Servicios:</strong> {{servicios}}</div>
                <div><strong>Importe Salón:</strong> ${{importe_salon}}</div>
                <div><strong>Importe Servicios:</strong> ${{importe_servicios}}</div>
                <div><strong>Total:</strong> ${{importe_total}}</div>
            </div>
        </div>
        {{/each}}
    {{else}}
        <div class="no-data">
            No hay reservas para el período seleccionado
        </div>
    {{/if}}

    {{#if reservas.length}}
    <div class="totales">
        <h3>Resumen General</h3>
        <p>Total Reservas: {{totalReservas}} | Ingresos Totales: ${{totalIngresos}}</p>
    </div>
    {{/if}}
</body>
</html>